# Roop Engine

AI に実装を任せるループを、テストの自己申告や権限逸脱に頼らず運用するための実験的な実行基盤です。

このプロジェクトでは、仕様と受け入れ条件を作る **プランナー**、関門を機械的に適用する **ランナー**、隔離環境内で実装する **ソルバー**を分離します。合格基準を作る主体と、それを満たす主体を分け、テストが実装前に失敗すること（Red）と実装後に成功すること（Green）の両方を確認します。

> [!IMPORTANT]
> 現在は設計と WSL2 サンドボックスのプロビジョニングまで完了しています。ループを制御するランナー本体は未実装です。

## 仕組み

| 役割 | 想定する実体 | 責務 |
|---|---|---|
| プランナー | Windows 側の Claude Code | 仕様、受け入れ条件、実行計画を作る。実装コードは書かない |
| ランナー | 通常のプログラム（未実装） | Red、テスト凍結、検証、リトライ、エスカレーションを機械的に管理する |
| ソルバー | WSL2 内の Codex CLI | 渡されたブリーフを基に実装する。テストや計画にはアクセスしない |

中心となる原則は次のとおりです。

- 受け入れ条件を作る者と実装する者を分ける
- 実装前にテストの Red を確認する
- 実装中はテストを凍結する
- リトライ上限への到達を、失敗ではなくプランナーへの差し戻しとして扱う
- ステップ間の情報はソースコードではなく contracts で受け渡す
- 人間への確認条件を事前に固定する
- ソルバーに sudo やホストへのアクセスを与えない

## 現在の状態

完了しているもの：

- ループの設計原則と開始プロンプト
- ランナーの詳細仕様
- WSL2 `Ubuntu-24.04` を使う隔離環境の設計
- `runner` / `solver` ユーザー、権限、SSH、ネットワーク制限のプロビジョニング
- Windows 側の起動、keepalive、Remote SSH に関する手順

未完了のもの：

- ランナー本体の実装
- ソルバー用 API 認証と非対話実行の検証
- egress 制限の適用と通信先の検証
- 最初に扱う題材の確定

最新の進捗と再開手順は [HANDOFF.md](HANDOFF.md) を参照してください。

## ドキュメント

初めて読む場合は、次の順番を推奨します。

1. [BOOTSTRAP.md](BOOTSTRAP.md) — 設計思想と、プランナーが作る成果物
2. [RUNNER_SPEC.md](RUNNER_SPEC.md) — ランナーの関門、状態遷移、CLI、データ形式
3. [provision/README.md](provision/README.md) — WSL2 サンドボックスの構築・検証手順
4. [host/README.md](host/README.md) — Windows 側の起動、keepalive、SSH 設定
5. [HANDOFF.md](HANDOFF.md) — 現在地、未完了項目、作業再開時の注意

## リポジトリ構成

```text
.
├── README.md             # このファイル
├── BOOTSTRAP.md          # プランナー向けの開始プロンプト
├── RUNNER_SPEC.md        # ループランナーの仕様
├── HANDOFF.md            # 進捗と引き継ぎ
├── host/
│   ├── README.md         # Windows ホスト側の手順
│   └── loop-dev.cmd      # 開発環境ランチャー
└── provision/
    ├── README.md         # サンドボックス構築手順
    ├── provision.sh      # プロビジョニングの入口
    └── *.sh              # 隔離、ユーザー、権限、Python、egress の設定
```

## 環境構築について

対象環境は Windows 上の WSL2 `Ubuntu-24.04` です。構築作業はホストや WSL の設定、SSH 鍵、ユーザー権限、ネットワーク制御を変更します。コマンドを抜粋して実行せず、先に [provision/README.md](provision/README.md) の前提、手順、既知の落とし穴を通して確認してください。

WSL2 は既定のままでは隔離環境ではありません。この構成では Windows ドライブの自動マウント、Windows 実行ファイルとの相互運用、WSLg を明示的に無効化し、プロビジョニング後に状態を検証します。

## 次の作業

[RUNNER_SPEC.md](RUNNER_SPEC.md) のライフサイクルと CLI 仕様に従ってランナー本体を実装することが、次の主要マイルストーンです。作業を再開する際は、先に [HANDOFF.md](HANDOFF.md) の「次にやること」と未決事項を確認してください。
